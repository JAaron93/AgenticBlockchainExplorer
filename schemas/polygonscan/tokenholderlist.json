
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://stablecoin-explorer/schemas/polygonscan/tokenholderlist.json",
  "title": "Polygonscan Token Holder List Response",
  "description": "JSON Schema for Polygonscan tokenholderlist API endpoint response",
  "version": "1.0.0",
  "$comment": "Strict root to enforce API envelope.",
  "type": "object",
  "required": ["status", "message", "result"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["0", "1"],
      "description": "API response status: 1 for success, 0 for error"
    },
    "message": {
      "type": "string",
      "description": "API response message"
    },
    "result": {
      "oneOf": [
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/tokenHolder"
          },
          "maxItems": 10000,
          "$comment": "Empty arrays valid (no holders). Max 10k items defines an operational safety upper bound."
        },
        {
          "type": "string",
          "description": "Error message or 'No token holder found'"
        }
      ]
    }
  },
  "definitions": {
    "tokenHolder": {
      "type": "object",
      "required": [
        "TokenHolderAddress",
        "TokenHolderQuantity"
      ],
      "properties": {
        "TokenHolderAddress": {
          "type": "string",
          "pattern": "^0x[a-fA-F0-9]{40}$",
          "description": "Holder wallet address"
        },
        "TokenHolderQuantity": {
          "type": "string",
          "pattern": "^[0-9]+$",
          "description": "Token balance in smallest unit"
        }
      },
      "additionalProperties": true,
      "$comment": "Intentionally open (true) to allow the API to return new/extra fields without breaking this client."
    }
  },
  "additionalProperties": false
}
